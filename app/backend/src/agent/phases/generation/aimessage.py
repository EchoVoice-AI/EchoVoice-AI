"""AI message handling for generation phase (placeholder)."""

def prepare_message(prompt: str, context: dict) -> dict:
    """Prepare an AI message for the generation phase."""
    return {"prompt": prompt, "context": context}
