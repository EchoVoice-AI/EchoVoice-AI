"""Experimentation LangGraph subgraph entry (placeholder)."""

def build_graph():
    """Build the experimentation phase LangGraph subgraph."""
    return {"name": "experimentation_graph", "nodes": []}
